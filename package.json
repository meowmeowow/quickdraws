{
  "name": "quickdraws",
  "version": "1.0.0",
  "description": "To activate the virtual environment:",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss -i project/static/styles.css -o project/static/output.css --watch"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.3"
  }
}

